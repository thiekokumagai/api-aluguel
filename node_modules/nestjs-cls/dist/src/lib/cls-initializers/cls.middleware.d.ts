import { NestMiddleware } from '@nestjs/common';
import { ClsMiddlewareOptions } from '../cls.options';
export declare class ClsMiddleware implements NestMiddleware {
    private readonly options;
    constructor(options: Omit<ClsMiddlewareOptions, 'mount'> | undefined);
    use: (req: any, res: any, next: (err?: any) => any) => Promise<void>;
}
//# sourceMappingURL=cls.middleware.d.ts.map